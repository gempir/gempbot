name: Provision

on: 
  workflow_dispatch:

jobs:
  deploy:
      name: Provision
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - name: Run ansible
          run: |
            echo "${{secrets.VAULT_PASSWORD}}" > ansible/.vaultpass
            make ansible
            make provision