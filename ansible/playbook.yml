---
- name: Provision hosts for spamchamp
  hosts: all
  remote_user: root
  
  roles:
    - {role: 'common', tags: ['common']}
    - {role: 'geerlingguy.redis', tags: ['redis']}
    - {role: 'ingester', tags: ['ingester']}
    - {role: 'caddy-ansible', tags: ['caddy']}
    - {role: 'api', tags: ['api']}
